pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Example of a shell command using the sh step
                sh 'echo "Hello, world!"'
            }
        }
    }
}

